name: ci

on:
  push:
    branches:
      - master

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Cache Docker layers
        uses: actions/cache@v2
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-
      - name: Login to Docker registry
        uses: docker/login-action@v1 
        with:
          registry: registry.lennart-austenfeld.de
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASSWORD }}
      - name: Build and load image
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          load: true
          tags: registry.lennart-austenfeld.de/tentris:latest
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,dest=/tmp/.buildx-cache
      - name: Push image
        run: docker push registry.lennart-austenfeld.de/tentris:latest
      - name: Checkout
        uses: actions/checkout@v1
      - name: debug
        run: ls -lah
      - name: Extract binaries
        run: ./.github/workflows/scripts/extract-binaries.sh registry.lennart-austenfeld.de/tentris:latest
      - name: Image digest
        run: echo ${{ steps.docker_build.outputs.digest }}
